import { Typography } from "@mui/material";
import React from "react";

export default function MyStays(props: any) {
  return (
    <React.Fragment>
      <Typography variant="h1">My Stays</Typography>
    </React.Fragment>
  );
}
